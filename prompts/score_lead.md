# Score lead

Score the lead using only the public data provided.

Return a compact JSON object with:

- score
- tier
- confidence
- score_reason

Use the campaign scoring model and keep the output deterministic when possible.

